apiVersion: v1
kind: Service
metadata:
  name: {{ include "oxn-platform.fullname" . }}
  labels:
    {{- include "oxn-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oxn-platform.selectorLabels" . | nindent 4 }}
